# Lists

# Forexample
a <- c(1,2,3,4, "A", "B")
a
# All are converted to characters
# To maintain the data type smae we use list in R

A <- list(1,2,3,4,5, "A", "Babar", TRUE, FALSE)
A
# Now data type is not changed. That's why list are used. 